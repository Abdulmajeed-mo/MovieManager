@* البيانات التي سيستقبلها هذا ال فيو هي *@
@model List<Movie>

<h2>Movies</h2>

@foreach (var movie in Model)
{
    <p >
        @movie.Title  -
        @movie.ReleaseDate -
        @movie.Rating
    </p>
}